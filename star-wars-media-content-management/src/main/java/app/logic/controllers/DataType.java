package app.logic.controllers;

/**
 * Represents an enum type for different types of data.
 * Data types originate from data context.
 * Usage of DataType enum is useful in UI.
 * @author jan.dostal
 */
public enum DataType 
{
    MOVIE,
    TV_SHOW,
    TV_SEASON,
    TV_EPISODE;
}
