package app.logic.controllers;

/**
 * Represents an enum type for different types of data sorting.
 * Data sorting types originate from data controllers.
 * Usage of DataSorting enum is useful in UI.
 * @author jan.dostal
 */
public enum DataSorting 
{
    LONGEST,
    NEWEST,
    BY_NAME,
    FAVORITE;
}
